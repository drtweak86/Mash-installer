[package]
name = "installer-fedora"
version = "0.1.4"
edition = "2021"
description = "Fedora-specific installer driver"
license = "MIT"

[dependencies]
installer-core = { path = "../installer-core" }
